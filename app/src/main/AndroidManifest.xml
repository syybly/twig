<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rmit.twig">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".View.PreferenceAcitivity"
            android:label="@string/title_activity_preference_acitivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".View.AdminWelcomeActivity" />
        <activity android:name=".View.MentorWelcomeActivity" />
        <activity android:name=".View.StudentWelcomeActivity" />
        <activity android:name=".View.SignupActivity" />
        <activity android:name=".View.ResetPasswordActivity" />
        <activity android:name=".View.ForgetPasswordActivity" />
        <activity android:name=".View.SentEmailActivity">

        </activity>
        <activity android:name=".View.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>